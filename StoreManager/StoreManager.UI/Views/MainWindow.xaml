<Window x:Class="StoreManager.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="سیستم مدیریت فروشگاه" Height="720" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        FontFamily="Vazir, Segoe UI">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="210"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource SidebarBrush}">
            <DockPanel>
                <!-- Logo / Store Name -->
                <Border DockPanel.Dock="Top" Padding="16,20" Background="#0A3880">
                    <StackPanel>
                        <TextBlock x:Name="txtStoreName" Text="فروشگاه من"
                                   Foreground="White" FontSize="16" FontWeight="Bold"
                                   TextAlignment="Center"/>
                        <TextBlock x:Name="txtStoreType" Text="مدیریت فروشگاه"
                                   Foreground="#90CAF9" FontSize="11" TextAlignment="Center"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Menu Items -->
                <StackPanel DockPanel.Dock="Top" Margin="8,12">
                    <Button Style="{StaticResource MenuButton}" Click="BtnDashboard_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📊" Margin="0,0,8,0"/>
                            <TextBlock Text="داشبورد"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MenuButton}" Click="BtnNewSale_Click" Margin="0,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🛒" Margin="0,0,8,0"/>
                            <TextBlock Text="فروش جدید"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MenuButton}" Click="BtnInvoices_Click" Margin="0,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📋" Margin="0,0,8,0"/>
                            <TextBlock Text="فاکتورها"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MenuButton}" Click="BtnProducts_Click" Margin="0,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📦" Margin="0,0,8,0"/>
                            <TextBlock Text="کالا و موجودی"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MenuButton}" Click="BtnCustomers_Click" Margin="0,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👥" Margin="0,0,8,0"/>
                            <TextBlock Text="مشتریان"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MenuButton}" Click="BtnReports_Click" Margin="0,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📈" Margin="0,0,8,0"/>
                            <TextBlock Text="گزارش‌ها"/>
                        </StackPanel>
                    </Button>

                    <Separator Background="#1976D2" Margin="0,8"/>

                    <Button Style="{StaticResource MenuButton}" Click="BtnSettings_Click" Margin="0,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚙️" Margin="0,0,8,0"/>
                            <TextBlock Text="تنظیمات"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Bar -->
            <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                <DockPanel Margin="16,0">
                    <TextBlock x:Name="txtPageTitle" Text="داشبورد"
                               FontSize="16" FontWeight="Bold"
                               VerticalAlignment="Center"
                               Foreground="{StaticResource PrimaryBrush}"/>
                    <TextBlock DockPanel.Dock="Left" x:Name="txtDateTime"
                               FontSize="12" Foreground="{StaticResource SubTextBrush}"
                               VerticalAlignment="Center" HorizontalAlignment="Left"/>
                </DockPanel>
            </Border>

            <!-- Page Frame -->
            <Frame x:Name="MainFrame" Grid.Row="1" NavigationUIVisibility="Hidden"/>
        </Grid>
    </Grid>
</Window>
